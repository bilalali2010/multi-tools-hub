name: Keep Streamlit App Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -s https://ai-tool-suite.streamlit.app/ > /dev/null
